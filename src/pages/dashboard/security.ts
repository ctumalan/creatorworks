import type { APIRoute } from 'astro';
import { accountSession } from '../../server/account-session';
import { signInDescription, sessionLabel, validProof } from '../../server/account-security.mjs';
import { env, workos } from '../../server/auth';
import { isFounder } from '../../server/admin-policy.mjs';
import { database, ensureMember } from '../../server/database';
import { surface, signIn, e } from '../../server/feedback-ui';
const messages:Record<string,string>={'invalid-email':'Enter a valid email address.',session:'The selected session was signed out.',reset:'Password-reset email requested. Check your inbox and spam folder.',provider:'Manage your password with your sign-in provider.',wait:'Please wait a minute before requesting another email.',confirm:'Please complete the confirmation before continuing.',reauth:'Confirm your identity by signing in again before requesting deletion.',founder:'The founder account cannot request deletion here. Administrator responsibility must be resolved first.',requested:'Your deletion request is pending. Your account has not been deleted yet.',cancelled:'Your deletion request was cancelled.',error:'The action could not be completed. No deletion has occurred. Please reload and try again; some sessions may already have been signed out.'};
export const GET:APIRoute=async context=>{
 try {
  const session=await accountSession(context);
  if(!session)return signIn('/dashboard/security');
  const admin=isFounder(session.user.id,env('FOUNDER_WORKOS_USER_ID'));
  let deletionReady=false,pending=false,consented=false;
  try{const member=await ensureMember(session.user);const r=await database().from('account_deletion_requests').select('status,erasure_consent').eq('user_id',member.id).maybeSingle();deletionReady=!r.error;pending=r.data?.status==='pending';consented=r.data?.erasure_consent===true;}catch{}
  const fresh=validProof(context.cookies.get('cw_security_fresh')?.value,session.user.id,'fresh',env('WORKOS_COOKIE_PASSWORD'));
  let sessionsHtml='<p>Sessions could not be loaded. You can still sign out of this browser below.</p>';
  try { const rows=await (await workos().userManagement.listSessions(session.user.id)).autoPagination(); sessionsHtml=rows.filter(s=>s.userId===session.user.id&&s.status==='active').map(s=>`<article class="session-row"><div><p><strong>${e(sessionLabel(s.userAgent))}</strong>${s.id===session.sessionId?' · This session':''}</p><p class="cw-meta">Signed in ${e(new Date(s.createdAt).toLocaleDateString('en-US'))}</p><details><summary>Details</summary><p>${e(signInDescription(s.authMethod))} · ${e(s.userAgent||'Browser details unavailable')}</p></details></div><form method="post" action="/api/account-security"><input type="hidden" name="action" value="revoke-session"><input type="hidden" name="sessionId" value="${e(s.id)}"><button class="secondary-button">Sign out</button></form></article>`).join('') || '<p>No active sessions returned.</p>'; } catch {}
  const notice=messages[context.url.searchParams.get('notice')||''];
  const providerLink=session.authenticationMethod==='GoogleOAuth'?'<a class="secondary-button" href="https://myaccount.google.com/security" target="_blank" rel="noopener noreferrer">Google account security ↗</a>':session.authenticationMethod==='GitHubOAuth'?'<a class="secondary-button" href="https://github.com/settings/security" target="_blank" rel="noopener noreferrer">GitHub account security ↗</a>':'';
  const reauth='<a class="secondary-button" href="/auth/sign-in?next=%2Fdashboard%2Fsecurity&amp;reauth=1">Confirm identity to continue</a>';
  return surface('Account & security',`<header><h1>Account &amp; security</h1></header>${notice?`<p class="cw-notice" role="status">${e(notice)}</p>`:''}<section class="cw-panel"><h2>Sign-in details</h2><p><strong>Email</strong><br>${e(session.user.email)}</p><p><strong>Current sign-in method</strong><br>${e(signInDescription(session.authenticationMethod))}</p><p class="cw-meta">Your public name and biography are managed in <a href="/dashboard/profile">Edit profile</a>. To change your email, confirm your identity first. You will sign in again and verify your new email address.</p>${fresh?'<form action="/api/account-security" method="post"><input type="hidden" name="action" value="change-email"><label for="new-email">New email</label><input id="new-email" type="email" name="email" maxlength="254" autocomplete="email" required><button class="secondary-button">Change email and verify</button></form>':reauth}</section><section class="cw-panel"><h2>Password</h2>${session.authenticationMethod==='Password'?'<p>We’ll email a secure link to your account email. Choose your new password on the secure sign-in page, not in chat.</p><form action="/api/account-security" method="post"><input type="hidden" name="action" value="password-reset"><button class="secondary-button">Send password-reset email</button></form>':`<p>Your current sign-in does not use a TryMyBuild password. Manage credentials with your sign-in provider; email-code users do not need a password.</p>${providerLink}`}</section><section class="cw-panel"><h2>Active sessions</h2><div class="session-list">${sessionsHtml}</div><p class="cw-meta">Revoked sessions may remain usable until their current access tokens expire.</p><div class="session-controls"><form action="/auth/sign-out" method="post"><button class="secondary-button">Sign out of this browser</button></form><details class="cw-security-details"><summary>Sign out of all TryMyBuild sessions</summary><p>This includes this browser. It does not sign you out of Google or other providers. Other sessions may remain usable until their existing access tokens expire.</p><form action="/api/account-security" method="post"><input type="hidden" name="action" value="revoke-all"><label class="cw-choice"><input type="checkbox" name="confirm" value="yes" required>Sign out all my TryMyBuild sessions</label><button class="secondary-button">Confirm sign out</button></form></details></div></section><section class="cw-panel"><h2>Delete account</h2>${admin?'<p>Your founder account is protected from self-service deletion to avoid losing administrator access.</p>':!deletionReady?'<p>Deletion requests are not connected yet. No request can be submitted until setup is complete.</p>':pending&&consented?`<p>Your request is pending administrator review. Nothing has been deleted. You can still use your account.</p>${fresh?'<form action="/api/account-security" method="post"><input type="hidden" name="action" value="cancel-deletion"><button class="secondary-button">Cancel deletion request</button></form>':reauth}`:`<p>Deletion is completed by the administrator after your request. Submitting a request does not delete or hide anything immediately.</p><p>Deletion removes your sign-in identity and photo, redacts and unpublishes your projects, removes uploaded previews and saved items, and redacts your contributions. Other people’s contributions and necessary audit records remain. Deletion cannot be undone. You may cancel until processing begins.</p>${fresh?'<form action="/api/account-security" method="post"><input type="hidden" name="action" value="request-deletion"><label for="delete-confirm">Type DELETE to request account deletion</label><input type="text" id="delete-confirm" name="confirmation" pattern="DELETE" required autocomplete="off"><label class="cw-choice"><input type="checkbox" name="confirm" value="yes" required>I authorize deletion of my account and data as described above.</label><button class="secondary-button">Request account deletion</button></form>':reauth}`}</section>`,'security',200,admin);
 }catch{return surface('Account & security','<h1>Account &amp; security</h1><p role="alert">Account details could not be loaded.</p><a href="/dashboard/security">Try again</a>','security',503);}
};
