# September 12 release checkpoint

The owner authorized committing, pushing, deploying, and publishing all approved website changes.

## Included

All 22 dashboard/catalog refinements and the two wish-category corrections described in `UI_REFINEMENTS_2026-09-12.md`, together with their tests and database migrations. Existing local design exports and unrelated historical audit/prompt documents were left untouched and excluded from the website release.

## Verification

- 176 application tests passed, TypeScript checks passed, and the production build passed immediately before release preparation.
- Disposable database checks passed for inbox sorting/pagination/unread state, draft-deletion authorization and history retention, custom-category persistence/deduplication/moderation, more than 200 wishes, role permissions, and erasure.
- Diff whitespace and the new read-only schema-check script's syntax checks passed.
- GitHub main matched starting revision `5ec88ef7540086a78dffb373417f7b7883faeb2b` before this release.
- Vercel's project API confirmed no Git integration link; there are no tracked GitHub Actions workflows. A push does not publish through these mechanisms.

## Verified production target and pending database changes

- GitHub: `ctumalan/trymybuild`.
- Vercel project: `trymybuild`, `prj_juKQHNOZiMyxCIufITAvivwQ50vm`, team `team_raCIieg8AhGENhA5TTkJoGis`.
- Existing production: `dpl_667KA6WnX1BNmcF8D8V2sZ1oC3j4`, Ready, serving `https://trymybuild.com` and `https://www.trymybuild.com`.
- Supabase: `creatorworks`, production branch, project `nkrkmfszuntvzjonrznb`, Tumalan Music organization.

A read-only query in the actual Supabase SQL editor confirmed that all six release functions (`cw_submit_wish`, `cw_review_wish`, `cw_workspace_inbox`, `cw_project_unread`, `cw_delete_unused_draft`, `cw_wish_categories`) and all three checked columns (`community_wishes.moderation_status`, `community_wishes.revision`, `account_preferences.selected_interests`) are absent. At that check there were zero wishes and one account-preferences record.

The required changes are `019_launch_safety.sql`, `020_workspace_refinements.sql`, and `021_wish_categories.sql`, in that order. None was applied during this attempt. A current recovery backup could not be verified because the browser connection repeatedly timed out and the native dashboard controls stopped responding. No customer rows were exported, edited, deleted, or moderated.

Vercel exposes the configuration names but redacts sensitive values when downloading production settings. The application service credential is not a PostgreSQL migration credential. Do not work around the migration checkpoint by deploying the new app against the old schema, guessing credentials, or exposing a privileged SQL endpoint.

## Resume

1. Reconnect the Supabase dashboard and verify the exact project above.
2. Recheck live schema and obtain a fresh backup/recovery route.
3. Apply only missing migrations in order; inspect permissions, function definitions, trigger installation, and preserved data.
4. Deploy the committed source to the existing Vercel project; keep historical local exports and environment files out of the deployment archive.
5. Verify the production deployment and public/authenticated routes before reporting publication as complete.

Search indexing and identity launch gates remain unchanged. This checkpoint is not an official-launch or authentication-readiness claim.
