# TryMyBuild launch-safety release checkpoint

September 11, 2026. The owner authorized commit, push, deployment, and publication.

## Validated release contents

This release contains the approved slogan, launch-audit safety fixes, moderated wishes, separate selected interests, self-hosted fonts, and regression tests. See LAUNCH_REMEDIATION_LOCAL.md and DATABASE_VALIDATION_2026-09-11.md for the implementation and isolated database checks.

Immediately before release, all 158 application tests, TypeScript checks, the production build, and the diff whitespace check passed. The remote main branch matched the starting commit 45428030e5e5f8b489bf36a2ea8b54b1136c1016. Unrelated untracked documents and output were excluded.

## Verified production target

- Repository: https://github.com/ctumalan/trymybuild
- Vercel: trymybuild, project prj_juKQHNOZiMyxCIufITAvivwQ50vm, team team_raCIieg8AhGENhA5TTkJoGis.
- Current production deployment before this release: dpl_667KA6WnX1BNmcF8D8V2sZ1oC3j4, Ready, serving https://trymybuild.com and https://www.trymybuild.com.
- Vercel's project API returned no Git integration link, so a repository push does not itself release this code through that integration. No repository Actions directory exists.
- Required database: Supabase project nkrkmfszuntvzjonrznb.

## Deployment held at the database checkpoint

The browser/dashboard connection repeatedly timed out. No Supabase management connector or configured database command-line connection was available. Vercel's configuration names contain the application service credential, not a PostgreSQL administrator connection; application data access is not a substitute for migration access.

The historical September 6 database archive exists, but it is not a fresh pre-release backup. A current backup and production schema check were therefore not completed. Migration 019 has NOT been applied, and the matching application has NOT been deployed. The production site remains unchanged.

Resume with authorized Supabase access: verify the exact project and current schema, create and inspect a fresh backup, apply database/019_launch_safety.sql, verify its columns and restricted function permissions, then deploy this release to the existing Vercel project and check the live APIs and browser journeys. Apply the migration before deploying the application.

The migration preserves wishes but makes existing wishes pending until reviewed. Prefer a forward fix if the release has a problem: blindly returning to the older application's wish endpoint could expose pending wishes because it lacks the moderation filter.

Search indexing remains intentionally disabled; WorkOS production identity continuity and the remaining hosted sign-up/email/account journey checks are still separate launch gates. This checkpoint is not a claim that the official public launch is complete.
